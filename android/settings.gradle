pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url 'http://192.168.0.17:8082/nexus/content/repositories/android-snapshots'
            credentials {
                username = 'dev'
                password = 'dev123'
                allowInsecureProtocol = true
            }
        }
    }
}
rootProject.name = "android"
include ':app'
